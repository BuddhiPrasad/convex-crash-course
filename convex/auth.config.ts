const authConfig = {
    providers: [
        {
          domain: "https://factual-sailfish-2.clerk.accounts.dev",
          applicationID: "convex",
        },
      ],
};

export default authConfig;